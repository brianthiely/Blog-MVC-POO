<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/a6b7960de6.js" crossorigin="anonymous"></script>
    <link rel="icon" href="/public/img/favicon/icons8-étoile-3d-fluency-16.png" />
    <title>{% block title %}Dashboard | Admin {% endblock %}</title>
</head>

<body>
{% block header %}
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-warning p-3">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class=" collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav mr-auto ">
                        <li class="nav-item">
                            <a class="nav-link mx-2 fw-bold" aria-current="page" href="/admin">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link mx-2 fw-bold" aria-current="page" href="/">Go to blog</a>
                        </li>
                        {% if session.get('user') %}
                            <li class="nav-item">
                                <a class="nav-link mx-2 fw-bold text-danger" href="/user/logout">Logout</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link mx-2 fw-bold text-warning" href="/user/login">Login</a>
                            </li>
                        {% endif %}

                    </ul>
                </div>
            </div>
        </nav>
    </header>
{% endblock %}

{% block flash %}
    {% if flash.has('error') %}
        <div class="alert alert-danger w-50 m-auto text-center" role="alert">
            {{ flash.get('error') }}
        </div>
    {% endif %}

    {% if flash.has('success') %}
        <div class="alert alert-success w-50 m-auto text-center" role="alert">
            {{ flash.get('success') }}
        </div>
    {% endif %}

    {% if flash.has('message') %}
        <div class="alert alert-warning w-50 m-auto text-center" role="alert">
            {{ flash.get('message') }}
        </div>
    {% endif %}
{% endblock %}

{% block body %}
{% endblock %}
</body>
</html>
